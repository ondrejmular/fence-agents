name = "fence_rsa test config for non admin user"
replay_server_type = "telnet"
subdir = "telnet-user" # subdir for mitm logs, e.g.: different firmware for one agent (not required)
agent_type = "mitmproxy"
agent = "fence_rsa"

[options] # options for fence-agent
    # option = [ "value", "long-opt", "short-opt" ]
        # option: name of the option which is used in fence-agent stdin
        # short-opt: not required
	login = [ "USERID", "--username", "-l" ]
	passwd = [ "PASSW0RD", "--password", "-p" ]
	ipaddr = [ "localhost", "--ip", "-a" ]
	ipport = [ "23", "--ipport", "-u" ]
